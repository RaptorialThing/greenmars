<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Main</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
        <% if current_user %>
        <a class="nav-link active" aria-current="page" href=<%=user_path(current_user)%>>Home</a> 
        <% else %>
        <a class="nav-link active" aria-current="page" href=<%=new_user_session_path%>>Home</a>
        <% end %>
            
          </li>
          <li class="nav-item">
            <a class="nav-link" href=<%=orders_path%>>Orders</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              My orders
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href=<%= new_order_path%>>New order</a></li>
              <li><hr class="dropdown-divider"></li>
              <% if current_user %>
              <li><a class="dropdown-item" href=<%= user_path(current_user)%>>My orders</a></li>
              <% else %>
              <li><a class="dropdown-item" href=<%= new_user_session_path %>>My orders</a></li>
              <% end %>
            </ul>
          </li>
         <% if current_user %>
            <li class="nav-item"><%= link_to "Log out", destroy_user_session_path,  data: { "turbo-method": :delete }, class: "nav-link" %></li>
            <% else %>
            <li class="nav-item"><a class="nav-link" href=<%= new_user_session_path %>>Log in</a></li>
            <% end %>
        </ul>
        <form class="d-flex">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
      </div>
    </div>
  </nav>